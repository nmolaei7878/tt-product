{
  "name": "app",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && npm install && npm start",
    "start:frontend": "cd frontend/toman && npm install && npm start",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
